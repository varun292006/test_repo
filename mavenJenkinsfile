pipeline {
    agent any
    stages {
        stage("source") {
            steps {
                git 'https://github.com/varun292006/test_repo.git'
            }
        }
        stage("Build") {
            script {
                cd maven/HelloWorldApp
                mvn package
            }
        }
    }
}
